[pytest]
norecursedirs = tests/manual
# Configuración de pytest para Fase 3

# Rutas de tests
testpaths = tests
pythonpath = .

# Patrones de archivos de test
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers personalizados
markers =
    unit: Unit tests (servicios, repositorio, cache)
    integration: Integration tests (endpoints HTTP)
    slow: Tests que tardan mucho
    smoke: Smoke tests básicos

# Configuración de output
addopts =
    --strict-markers
    --tb=short
    -ra

# Configuración de coverage
[coverage:run]
source = backend
omit =
    */tests/*
    */site-packages/*
    */__pycache__/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod

# Fail if coverage is below threshold (currently informational)
# Uncomment fail_under when coverage reaches target
# fail_under = 60
show_missing = true
precision = 2
